apiVersion: v1
kind: Service
metadata:
  name: {{ include "sokcho-planner.fullname" . }}
  labels:
    {{- include "sokcho-planner.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "sokcho-planner.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
